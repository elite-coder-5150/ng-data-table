import { IsCheckedDirective } from './is-checked.directive';

describe('IsCheckedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsCheckedDirective();
    expect(directive).toBeTruthy();
  });
});
